# Phase 22.3: Review Fixes

**Date:** 2026-03-10
**Phase:** 22.3 follow-up (implementation review fixes)  
**Status:** Pass 1 and Pass 2 actionable review findings are addressed in code; buffered decompression remains a documented deferred limitation, Unity/device validation still pending

## What Was Fixed

This pass closes the actionable findings from the updated Phase 22.3 review set:

1. Restored the missing `System.Threading` imports in `AdaptiveInterceptor.cs` and `BackgroundNetworkingPolicy.cs` so the Core assembly compiles again under Unity 2021.3 / C# 8.
2. Corrected retry terminal-attempt telemetry, and stopped retry loops from continuing after a retryable transport error was already delivered on a committed response path.
3. Hardened decompression with broader exception handling, a decompressed-size limit, multi-value `Content-Encoding` support, and `x-gzip` alias handling while preserving unsupported encodings untouched.
4. Removed the redirect completion-bridge hang when a spawned redirect dispatch completes successfully without ever delivering `OnResponseEnd(...)` or `OnResponseError(...)`.
5. Reduced hot-path allocations in observability by making logging preview allocation lazy, removing monitor `_responseBody?.ToArray()` capture, and caching `ReadOnlySequenceStream.Position`.
6. Bound cache background store/revalidation work to interceptor disposal via cancellation, and explicitly observed background tasks so faults cannot escape as unobserved task exceptions.
7. Fixed the remaining clone-ownership leaks on synchronous `next(...)` throws, wired `GracePeriodBeforeQueue` into background networking, and removed the `Cookie` merge `Split(';')` array allocation.
8. Added targeted regressions for decompression truncation/limits/encoding chains, retry committed-error behavior, redirect completion bridging, metrics 3xx classification, cache disposal cancellation, and background scope budget behavior.

## Files Modified

| File | Change |
|------|--------|
| `Runtime/Core/AdaptiveInterceptor.cs` | Restored `using System.Threading;` earlier and added clone-disposal protection on synchronous dispatch throws. |
| `Runtime/Core/BackgroundNetworkingPolicy.cs` | Restored `using System.Threading;` earlier for `BackgroundRequestQueuedException`. |
| `Runtime/Retry/RetryInterceptor.cs` | Fixed terminal-attempt telemetry and committed-error retry behavior. |
| `Runtime/Retry/RetryDetectorHandler.cs` | Prevented committed retryable error paths from silently continuing without terminal delivery. |
| `Runtime/Middleware/DecompressionInterceptor.cs` | Added max decompressed-body configuration and clone-disposal protection. |
| `Runtime/Middleware/DecompressionHandler.cs` | Added decompression-size limiting, broader error handling, multi-value decoding, and `x-gzip` support. |
| `Runtime/Middleware/RedirectHandler.cs` | Added completion-bridge protection for success-without-callback dispatches. |
| `Runtime/Core/Internal/ReadOnlySequenceStream.cs` | Removed O(n) `Position` calculation. |
| `Runtime/Observability/LoggingHandler.cs` | Lazy body-preview allocation. |
| `Runtime/Observability/MetricsHandler.cs` | 3xx statuses no longer count as failed requests. |
| `Runtime/Observability/MonitorHandler.cs` | Removed `_responseBody?.ToArray()` response capture. |
| `Runtime/Observability/MonitorInterceptor.cs` | Reduced snapshot allocations and made capture-throttle state atomic-safe. |
| `Runtime/Auth/AuthInterceptor.cs` | Clone disposal on synchronous dispatch throw. |
| `Runtime/Middleware/DefaultHeadersInterceptor.cs` | Clone disposal on synchronous dispatch throw. |
| `Runtime/Middleware/CookieInterceptor.cs` | Clone disposal on synchronous dispatch throw and allocation-free token splitting. |
| `Runtime/Middleware/RedirectInterceptor.cs` | Best-effort request-body reuse on redirect hops. |
| `Runtime/Core/BackgroundNetworkingInterceptor.cs` | Removed exception-filter `scope` access and wired in `GracePeriodBeforeQueue`. |
| `Runtime/Cache/CacheInterceptor.cs` | Disposal-bound cancellation and observation for background store/revalidation tasks. |
| `Tests/Runtime/Middleware/DecompressionInterceptorTests.cs` | Added truncation, size-limit, `x-gzip`, and multi-value encoding regressions. |
| `Tests/Runtime/Retry/RetryInterceptorTests.cs` | Added committed-response retry regression. |
| `Tests/Runtime/Middleware/RedirectInterceptorTests.cs` | Added completion-bridge regression. |
| `Tests/Runtime/Observability/MetricsInterceptorTests.cs` | Added 3xx classification regression. |
| `Tests/Runtime/Core/BackgroundNetworkingTests.cs` | Updated fake scope budget behavior. |
| `Tests/Runtime/Cache/CacheInterceptorTests.cs` | Added disposal-cancellation regression for background revalidation. |

## Specialist Review Re-Run

Both required rubrics were re-run explicitly against the review-fix diff:

- `.claude/agents/unity-infrastructure-architect.md`
- `.claude/agents/unity-network-architect.md`

## Findings Closed

- **Pass 1:** C-1, C-2, H-1, M-1, M-2
- **Pass 2:** H-2, H-3, H-5, H-6, H-7, H-8, H-9, M-3, M-4, M-5, M-6, M-7, M-8, M-9, M-10, L-1, L-2, L-3, L-4

## Deferred / Remaining

| ID | Severity | Description | Target |
|----|----------|-------------|--------|
| H-4 | HIGH | `DecompressionHandler` still buffers the full compressed body before decompression. This remains the documented Phase 22 limitation and needs a larger streaming-design follow-up. | Follow-up / v1.1 |
| V-1 | HIGH | Unity Editor compile/test execution has not been run from this workspace | Phase 22 validation |
| V-2 | HIGH | IL2CPP/mobile validation for redirect, retry, cache, decompression, and background work remains pending | Phase 22 validation |

## Validation

- `git diff --check` passes after the review fixes.
- Re-ran static review checks for the updated decompression, retry, redirect, observability, background, and cache paths.
- Added targeted regressions for:
  - decompression truncation, decompressed-size limits, `x-gzip`, and multi-value encoding chains
  - committed-response retry behavior
  - redirect completion-bridge safety
  - metrics 3xx classification
  - cache background revalidation cancellation on dispose
- Not completed: Unity compile/test execution or device validation. The workspace still has no runnable `.sln`/`.csproj` or Unity batch runner entrypoint.

## Remaining Work

1. Run the updated runtime/editor tests in Unity Test Runner.
2. Validate redirect/retry/cache/decompression/background behavior on IL2CPP/mobile targets.
3. Track the deferred decompression streaming follow-up (H-4) in the next relevant phase.
